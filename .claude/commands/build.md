Build the project. Run the language-appropriate build command:
- Go: `go build ./...`
- TypeScript: `npm run build`

Then run the linter:
- Go: `golangci-lint run`
- TypeScript: `npx biome check .`

Report any errors clearly.
